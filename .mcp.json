{
  "mcpServers": {
    "refero": {
      "type": "http",
      "url": "https://api.refero.design/mcp",
      "headers": {
        "Authorization": "Bearer ${REFERO_API_KEY}"
      }
    },
    "notion": {
      "command": "npx",
      "args": ["-y", "@notionhq/notion-mcp-server"],
      "env": {
        "NOTION_TOKEN": "${NOTION_TOKEN:-}"
      }
    }
  }
}
